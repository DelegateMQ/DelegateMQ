# Copied here at build time from src/delegate-mq/ (see Makefile) -- not this
# sample's own sources, never commit them.
/NuttXThread.cpp
/Timer.cpp
/Fault.cpp
/Make.dep

# NuttX's own build bookkeeping marker files (created directly in this
# directory since CONFIG_APPS_DIR points here).
/.context
/.dirlinks
